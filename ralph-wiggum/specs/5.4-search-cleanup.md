# Spec: Phase 5.4 - Search & Cleanup

> **Goal:** Implement server-side search and clean up UI artifacts.

## 1. Server-Side Session Search
**File:** `src/app/api/sessions/route.ts`
- **Requirement:** Support `?q=query`.
- **Action:**
  - Parse `q` search param.
  - Pass to `sessionLoader.listSessions(limit, offset, query)`.
  - Update `session-loader` implementation to filter by filename/metadata if query exists.

**File:** `src/features/sessions/components/link-session-dialog.tsx`
- **Action:**
  - Add search input.
  - Use `useDebounce` (300ms) to update search state.
  - Pass search term to `useSessions(query)` hook.

## 2. Status Filter Cleanup
**File:** `src/features/boards/components/board-filter-controls.tsx`
- **Action:** Remove the "Status" dropdown (`Select`).
- **Context:** Kanban columns *are* the status filter, so this dropdown is redundant.

## 3. Final Verification
- [ ] Session search works for "login" (finds session files).
- [ ] Status dropdown is gone from board header.
- [ ] Full regression test of Phase 5 features.
